<mvc:View
  controllerName="mdm.md.mdmrules.manage.ext.controller.ValidationRuleList"
  xmlns:mvc="sap.ui.core.mvc"
  xmlns:core="sap.ui.core"
  xmlns="sap.m"
  height="100%">
  <Page
    title="Validation Rules"
    showNavButton="true"
    navButtonPress=".onBackToHub">
    <content>
      <!--
        A decision table, the same shape as the duplicate rule table: conditions decide whether the
        rule applies, then the rule itself. Read a row left to right as one sentence -
        "where Country is BE, Language must be = NL".
      -->
      <MessageStrip
        class="sapUiSmallMarginBeginEnd sapUiSmallMarginTop"
        visible="{= ${view>/skipped} > 0 }"
        text="{view>/skippedText}"
        type="Warning"
        showIcon="true" />
      <Table
        id="ruleTable"
        items="{ path: 'dc>/ValidationRules', parameters: { $orderby: 'sequence,createdAt', $$updateGroupId: 'ruleChanges' } }"
        mode="SingleSelectMaster"
        busy="{view>/busy}"
        growing="true"
        growingThreshold="100">
        <headerToolbar>
          <OverflowToolbar>
            <Title text="Rules" level="H2" />
            <ToolbarSpacer />
            <Button text="Add Rule" icon="sap-icon://add" type="Transparent" press=".onAddRule" />
            <Button text="Delete" icon="sap-icon://delete" type="Transparent" press=".onDeleteRule" />
            <Button text="Save" icon="sap-icon://save" type="Emphasized" press=".onSave" enabled="{view>/dirty}" />
            <Button text="Discard" icon="sap-icon://undo" type="Transparent" press=".onDiscard" enabled="{view>/dirty}" />
          </OverflowToolbar>
        </headerToolbar>
        <!-- Percentage widths: without them every cell shrinks to its content and the row reads as
             scattered controls with gaps between them. -->
        <columns>
          <Column width="14%"><Text text="Condition 1 Field" /></Column>
          <Column width="9%"><Text text="Condition 1 Value" /></Column>
          <Column width="14%"><Text text="Condition 2 Field" /></Column>
          <Column width="9%"><Text text="Condition 2 Value" /></Column>
          <Column width="16%"><Text text="Field" /></Column>
          <Column width="13%"><Text text="Comparison" /></Column>
          <Column width="11%"><Text text="Value" /></Column>
          <Column width="10%"><Text text="Severity" /></Column>
          <Column width="4rem" hAlign="Center"><Text text="Active" /></Column>
        </columns>
        <items>
          <ColumnListItem>
            <cells>
              <!-- Both conditions are optional and independent: leave a pair empty and it means
                   "any". A condition on the same section as the Field narrows the rule to the rows
                   that match, rather than to every row of a partner that has one. -->
              <Input
                width="100%"
                value="{dc>conditionField}"
                placeholder="any"
                showValueHelp="true"
                valueHelpRequest=".onFieldValueHelp"
                change=".onCellChange" />
              <Input
                width="100%"
                value="{dc>conditionValue}"
                placeholder="any"
                enabled="{= !!${dc>conditionField} }"
                change=".onCellChange" />
              <Input
                width="100%"
                value="{dc>conditionField2}"
                placeholder="any"
                showValueHelp="true"
                valueHelpRequest=".onFieldValueHelp"
                change=".onCellChange" />
              <Input
                width="100%"
                value="{dc>conditionValue2}"
                placeholder="any"
                enabled="{= !!${dc>conditionField2} }"
                change=".onCellChange" />
              <!-- The field being validated. The value help lists the whole staging model, so a
                   rule can only ever name a field a request can actually hold. -->
              <Input
                width="100%"
                value="{dc>field}"
                placeholder="choose a field"
                showValueHelp="true"
                valueHelpRequest=".onFieldValueHelp"
                change=".onCellChange" />
              <ComboBox
                width="100%"
                selectedKey="{dc>comparison}"
                items="{ path: 'opt>/comparisons', templateShareable: false }"
                change=".onCellChange">
                <core:Item key="{opt>code}" text="{opt>text}" />
              </ComboBox>
              <!-- Disabled for `is empty` / `is not empty`, which compare against nothing: a value
                   that would be silently ignored is worse than no cell at all. A value that names a
                   catalog field compares the two fields instead. -->
              <Input
                width="100%"
                value="{dc>value}"
                placeholder="value or field"
                enabled="{= ${view>/needsValue}[${dc>comparison}] !== false }"
                showValueHelp="true"
                valueHelpRequest=".onFieldValueHelp"
                change=".onCellChange" />
              <ComboBox
                width="100%"
                selectedKey="{dc>severity}"
                items="{ path: 'opt>/severities', templateShareable: false }"
                change=".onCellChange">
                <core:Item key="{opt>code}" text="{opt>text}" />
              </ComboBox>
              <CheckBox selected="{dc>isActive}" select=".onCellChange" />
            </cells>
          </ColumnListItem>
        </items>
      </Table>
    </content>
  </Page>
</mvc:View>
