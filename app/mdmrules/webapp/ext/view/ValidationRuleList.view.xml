<mvc:View
  controllerName="mdm.md.mdmrules.manage.ext.controller.ValidationRuleList"
  xmlns:mvc="sap.ui.core.mvc"
  xmlns:core="sap.ui.core"
  xmlns="sap.m"
  height="100%">
  <Page
    title="Validation Rules"
    showNavButton="true"
    navButtonPress=".onBackToHub">
    <content>
      <!--
        A decision table, the same shape as the duplicate rule table: conditions decide whether the
        rule applies, then the rule itself. Read a row left to right as one sentence -
        "where Country is BE, Language must be = NL".

        Five condition slots since 2026-09-01, two of them drawn until "Add Condition" reveals the
        next Logic/Condition pair - the same rollout Workflow Agent Determination got, and the same
        reasoning: the slots are real columns on the row, so this is a rendering decision rather
        than a schema one. See db/quality-rules.cds.
      -->
      <MessageStrip
        class="sapUiSmallMarginBeginEnd sapUiSmallMarginTop"
        visible="{= ${view>/skipped} > 0 }"
        text="{view>/skippedText}"
        type="Warning"
        showIcon="true" />
      <!-- Horizontal scrolling, so revealing a condition widens the table instead of squeezing every
           cell. `view>/tableWidth` is the other half of it: a fixed-layout table at `width="100%"`
           redistributes its columns into whatever space it has, which is the squashing itself. -->
      <ScrollContainer horizontal="true" vertical="false" width="100%">
      <!-- MultiSelect since 2026-09-02 (asked for): a checkbox as column 1, and the header's
           own checkbox selects every loaded row - Delete and Duplicate act on all of them.
           `sap.m.Table` draws both itself; there is no column to declare for it. -->
      <Table
        id="ruleTable"
        width="{view>/tableWidth}"
        items="{ path: 'dc>/ValidationRules', parameters: { $orderby: 'sequence,createdAt', $$updateGroupId: 'ruleChanges' } }"
        mode="MultiSelect"
        busy="{view>/busy}"
        growing="true"
        growingThreshold="100">
        <headerToolbar>
          <OverflowToolbar>
            <Title text="Rules" level="H2" />
            <ToolbarSpacer />
            <Button text="Add Rule" icon="sap-icon://add" type="Transparent" press=".onAddRule" />
            <Button text="Delete Rule" icon="sap-icon://delete" type="Transparent" press=".onDeleteRule" />
            <!-- Table-wide, not per row: it reveals the next Logic/Condition COLUMN pair for every
                 rule at once. Delete Condition removes the last one and clears it on every row, so
                 nothing is left matching in a column nobody can see. -->
            <Button
              text="Add Condition"
              icon="sap-icon://add-filter"
              type="Transparent"
              enabled="{= ${view>/conditions} &lt; ${view>/maxConditions} }"
              press=".onAddCondition" />
            <Button
              text="Delete Condition"
              icon="sap-icon://less"
              type="Transparent"
              enabled="{= ${view>/conditions} &gt; 1 }"
              press=".onDeleteCondition" />
            <Button text="Duplicate" icon="sap-icon://copy" type="Transparent" press=".onDuplicateRule" />
            <!-- The duplicate page's "Test Against Current BPs", for validations: how much of the
                 data that already exists these rules would flag. Runs the rules ON SCREEN, unsaved
                 ones included, because seeing that before switching a rule on is the whole point. -->
            <Button text="Check Current Data" icon="sap-icon://lab" type="Transparent" press=".onCheckCurrentData" />
            <ToolbarSeparator />
            <Button text="Export to Excel" icon="sap-icon://excel-attachment" type="Transparent" press=".onExportExcel" />
            <Button text="Import from Excel" icon="sap-icon://upload" type="Transparent" press=".onImportExcel" />
            <ToolbarSeparator />
            <Button text="Save" icon="sap-icon://save" type="Emphasized" press=".onSave" enabled="{view>/dirty}" />
            <Button text="Discard" icon="sap-icon://undo" type="Transparent" press=".onDiscard" enabled="{view>/dirty}" />
          </OverflowToolbar>
        </headerToolbar>
        <!-- Fixed widths in rem, not percentages: a hidden column contributes no share of 100%, so
             percentages would re-flow the whole row every time a condition was revealed. -->
        <columns>
          <Column width="24rem"><Text text="Condition 1" /></Column>
          <!-- Titleless on purpose: AND / OR / NOR reads as the joining word between the two
               conditions it sits between, and a heading over it would only repeat the values. -->
          <Column width="6rem" hAlign="Center" visible="{= ${view>/conditions} &gt;= 2 }"><Text text="" /></Column>
          <Column width="24rem" visible="{= ${view>/conditions} &gt;= 2 }"><Text text="Condition 2" /></Column>
          <Column width="6rem" hAlign="Center" visible="{= ${view>/conditions} &gt;= 3 }"><Text text="" /></Column>
          <Column width="24rem" visible="{= ${view>/conditions} &gt;= 3 }"><Text text="Condition 3" /></Column>
          <Column width="6rem" hAlign="Center" visible="{= ${view>/conditions} &gt;= 4 }"><Text text="" /></Column>
          <Column width="24rem" visible="{= ${view>/conditions} &gt;= 4 }"><Text text="Condition 4" /></Column>
          <Column width="6rem" hAlign="Center" visible="{= ${view>/conditions} &gt;= 5 }"><Text text="" /></Column>
          <Column width="24rem" visible="{= ${view>/conditions} &gt;= 5 }"><Text text="Condition 5" /></Column>
          <Column width="16rem"><Text text="Field" /></Column>
          <Column width="13rem"><Text text="Comparison" /></Column>
          <Column width="11rem"><Text text="Value" /></Column>
          <Column width="10rem"><Text text="Severity" /></Column>
          <Column width="4rem" hAlign="Center"><Text text="Active" /></Column>
        </columns>
        <items>
          <ColumnListItem>
            <cells>
              <!-- Condition 1: field, comparator, values, side by side in one column - the shape
                   Workflow Agent Determination uses (2026-09-02, asked for: "the other tiles should
                   have the exact same way of working"). Every slot below is the same three controls.

                   Every condition is optional and independent: leave one empty and it means "any".
                   A condition on the same section as the Field narrows the rule to the rows that
                   match, rather than to every row of a partner that has one. A blank comparator
                   reads as `=`, which is what every condition meant before there was one to pick.

                   `targetType: 'any'` on every `dc>` reference inside an expression binding: without
                   it UI5 formats the referenced property into the type of the BOUND property - a
                   Boolean here - and a String value throws a FormatException, leaving the cell at
                   its default. See CLAUDE.md, "Workflow Agent Determination". -->
              <HBox alignItems="Center">
                <Input
                  width="9rem"
                  value="{dc>conditionField}"
                  placeholder="any"
                  showValueHelp="true"
                  valueHelpRequest=".onFieldValueHelp"
                  change=".onCellChange" />
                <Select
                  width="6.5rem"
                  class="sapUiTinyMarginBeginEnd"
                  selectedKey="{dc>conditionOperator}"
                  items="{ path: 'opt>/comparisons', templateShareable: false }"
                  change=".onCellChange">
                  <core:Item key="{opt>code}" text="{opt>text}" />
                </Select>
                <Input
                  width="7rem"
                  value="{dc>conditionValue}"
                  placeholder="any, or Value1|Value2"
                  enabled="{= !!${path: 'dc>conditionField', targetType: 'any'} &amp;&amp; ${path: 'dc>conditionOperator', targetType: 'any'} !== 'empty' &amp;&amp; ${path: 'dc>conditionOperator', targetType: 'any'} !== 'notEmpty' }"
                  change=".onCellChange" />
              </HBox>
              <!-- Only enabled once BOTH of the conditions it joins are filled: there is nothing to
                   join otherwise, and NOR on a single condition would silently invert it. -->
              <ComboBox
                width="100%"
                selectedKey="{dc>conditionLogic}"
                enabled="{= !!${path: 'dc>conditionField', targetType: 'any'} &amp;&amp; !!${path: 'dc>conditionField2', targetType: 'any'} }"
                items="{ path: 'opt>/conditionLogics', templateShareable: false }"
                change=".onCellChange">
                <core:Item key="{opt>code}" text="{opt>text}" />
              </ComboBox>
              <HBox alignItems="Center">
                <Input
                  width="9rem"
                  value="{dc>conditionField2}"
                  placeholder="any"
                  showValueHelp="true"
                  valueHelpRequest=".onFieldValueHelp"
                  change=".onCellChange" />
                <Select
                  width="6.5rem"
                  class="sapUiTinyMarginBeginEnd"
                  selectedKey="{dc>conditionOperator2}"
                  items="{ path: 'opt>/comparisons', templateShareable: false }"
                  change=".onCellChange">
                  <core:Item key="{opt>code}" text="{opt>text}" />
                </Select>
                <Input
                  width="7rem"
                  value="{dc>conditionValue2}"
                  placeholder="any, or Value1|Value2"
                  enabled="{= !!${path: 'dc>conditionField2', targetType: 'any'} &amp;&amp; ${path: 'dc>conditionOperator2', targetType: 'any'} !== 'empty' &amp;&amp; ${path: 'dc>conditionOperator2', targetType: 'any'} !== 'notEmpty' }"
                  change=".onCellChange" />
              </HBox>
              <ComboBox
                width="100%"
                selectedKey="{dc>conditionLogic2}"
                enabled="{= !!${path: 'dc>conditionField2', targetType: 'any'} &amp;&amp; !!${path: 'dc>conditionField3', targetType: 'any'} }"
                items="{ path: 'opt>/conditionLogics', templateShareable: false }"
                change=".onCellChange">
                <core:Item key="{opt>code}" text="{opt>text}" />
              </ComboBox>
              <HBox alignItems="Center">
                <Input
                  width="9rem"
                  value="{dc>conditionField3}"
                  placeholder="any"
                  showValueHelp="true"
                  valueHelpRequest=".onFieldValueHelp"
                  change=".onCellChange" />
                <Select
                  width="6.5rem"
                  class="sapUiTinyMarginBeginEnd"
                  selectedKey="{dc>conditionOperator3}"
                  items="{ path: 'opt>/comparisons', templateShareable: false }"
                  change=".onCellChange">
                  <core:Item key="{opt>code}" text="{opt>text}" />
                </Select>
                <Input
                  width="7rem"
                  value="{dc>conditionValue3}"
                  placeholder="any, or Value1|Value2"
                  enabled="{= !!${path: 'dc>conditionField3', targetType: 'any'} &amp;&amp; ${path: 'dc>conditionOperator3', targetType: 'any'} !== 'empty' &amp;&amp; ${path: 'dc>conditionOperator3', targetType: 'any'} !== 'notEmpty' }"
                  change=".onCellChange" />
              </HBox>
              <ComboBox
                width="100%"
                selectedKey="{dc>conditionLogic3}"
                enabled="{= !!${path: 'dc>conditionField3', targetType: 'any'} &amp;&amp; !!${path: 'dc>conditionField4', targetType: 'any'} }"
                items="{ path: 'opt>/conditionLogics', templateShareable: false }"
                change=".onCellChange">
                <core:Item key="{opt>code}" text="{opt>text}" />
              </ComboBox>
              <HBox alignItems="Center">
                <Input
                  width="9rem"
                  value="{dc>conditionField4}"
                  placeholder="any"
                  showValueHelp="true"
                  valueHelpRequest=".onFieldValueHelp"
                  change=".onCellChange" />
                <Select
                  width="6.5rem"
                  class="sapUiTinyMarginBeginEnd"
                  selectedKey="{dc>conditionOperator4}"
                  items="{ path: 'opt>/comparisons', templateShareable: false }"
                  change=".onCellChange">
                  <core:Item key="{opt>code}" text="{opt>text}" />
                </Select>
                <Input
                  width="7rem"
                  value="{dc>conditionValue4}"
                  placeholder="any, or Value1|Value2"
                  enabled="{= !!${path: 'dc>conditionField4', targetType: 'any'} &amp;&amp; ${path: 'dc>conditionOperator4', targetType: 'any'} !== 'empty' &amp;&amp; ${path: 'dc>conditionOperator4', targetType: 'any'} !== 'notEmpty' }"
                  change=".onCellChange" />
              </HBox>
              <ComboBox
                width="100%"
                selectedKey="{dc>conditionLogic4}"
                enabled="{= !!${path: 'dc>conditionField4', targetType: 'any'} &amp;&amp; !!${path: 'dc>conditionField5', targetType: 'any'} }"
                items="{ path: 'opt>/conditionLogics', templateShareable: false }"
                change=".onCellChange">
                <core:Item key="{opt>code}" text="{opt>text}" />
              </ComboBox>
              <HBox alignItems="Center">
                <Input
                  width="9rem"
                  value="{dc>conditionField5}"
                  placeholder="any"
                  showValueHelp="true"
                  valueHelpRequest=".onFieldValueHelp"
                  change=".onCellChange" />
                <Select
                  width="6.5rem"
                  class="sapUiTinyMarginBeginEnd"
                  selectedKey="{dc>conditionOperator5}"
                  items="{ path: 'opt>/comparisons', templateShareable: false }"
                  change=".onCellChange">
                  <core:Item key="{opt>code}" text="{opt>text}" />
                </Select>
                <Input
                  width="7rem"
                  value="{dc>conditionValue5}"
                  placeholder="any, or Value1|Value2"
                  enabled="{= !!${path: 'dc>conditionField5', targetType: 'any'} &amp;&amp; ${path: 'dc>conditionOperator5', targetType: 'any'} !== 'empty' &amp;&amp; ${path: 'dc>conditionOperator5', targetType: 'any'} !== 'notEmpty' }"
                  change=".onCellChange" />
              </HBox>
              <!-- The field being validated. The value help lists the whole staging model, so a
                   rule can only ever name a field a request can actually hold. -->
              <Input
                width="100%"
                value="{dc>field}"
                placeholder="choose a field"
                showValueHelp="true"
                valueHelpRequest=".onFieldValueHelp"
                change=".onCellChange" />
              <ComboBox
                width="100%"
                selectedKey="{dc>comparison}"
                items="{ path: 'opt>/comparisons', templateShareable: false }"
                change=".onCellChange">
                <core:Item key="{opt>code}" text="{opt>text}" />
              </ComboBox>
              <!-- Disabled for `is empty` / `is not empty`, which compare against nothing: a value
                   that would be silently ignored is worse than no cell at all. A value that names a
                   catalog field compares the two fields instead.

                   The two operators are NAMED here rather than looked up in a served `needsValue`
                   map (2026-09-02): a flag that fails to arrive read as "this needs a value" and
                   made Check Current Data refuse a perfectly good `is not empty` rule. This is the
                   shape the Workflow page already uses. -->
              <Input
                width="100%"
                value="{dc>value}"
                placeholder="value or field"
                enabled="{= ${path: 'dc>comparison', targetType: 'any'} !== 'empty' &amp;&amp; ${path: 'dc>comparison', targetType: 'any'} !== 'notEmpty' }"
                showValueHelp="true"
                valueHelpRequest=".onFieldValueHelp"
                change=".onCellChange" />
              <ComboBox
                width="100%"
                selectedKey="{dc>severity}"
                items="{ path: 'opt>/severities', templateShareable: false }"
                change=".onCellChange">
                <core:Item key="{opt>code}" text="{opt>text}" />
              </ComboBox>
              <CheckBox selected="{dc>isActive}" select=".onCellChange" />
            </cells>
          </ColumnListItem>
        </items>
      </Table>
      </ScrollContainer>
    </content>
  </Page>
</mvc:View>
